# Implementation Plans

Generated by the improve skill on 2026-07-10 (audit at commit `dc84bd6`);
plans 011–016 added by the 2026-07-16 re-audit (fwbg-agents `75123b0`,
fwbg `origin/main 8ab08f7` — note the local fwbg checkout was 25 commits
behind origin/main on a stale feature branch at audit time; plans touching
fwbg branch from origin/main). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Enforce stop-loss + fail-closed criteria gates in code | P1 | M | — | PARTIAL — Steps 3+4 DONE (fail-closed criteria + float-cast guard); Steps 1+2 REJECTED (stop-loss is order-level → fwbg pre-trade validator, not strategy_validator) (advisor/001-enforce-deterministic-safety-gates) |
| 002  | Dependency manifest hygiene (pydantic-ai pin, anthropic, dead deps) | P1 | S | — | DONE (advisor/002-dependency-manifest-hygiene) |
| 003  | CI quality gates (mypy clean, scripts linted, coverage measured) | P2 | M | 002 | DONE (advisor/003-ci-quality-gates; coverage baseline 84%) |
| 004  | Interim hardening of the web→LLM→executable-code path | P1 | M | — | DONE (advisor/004-harden-llm-code-path) |
| 005  | Complete the plugin evaluator's invariants (NaN/inf, dtype) | P2 | M | 004 | DONE (advisor/005-complete-evaluator-invariants) |
| 006  | Agent-Run-Detailansicht (klickbare Agents, LLM-Session-Einblick) — Feature, cross-repo mit fwbg-dashboard | P2 | L | — | DONE — Steps 1–6 + 8–10 (fwbg-agents `feat/006-agent-run-detail`, fwbg-dashboard `feat/006-agent-run-detail`); Step 7 (parent_run_id) done in Plan 008 Schritt 5; plugin_evaluator scenario-counters done in Plan 008 Schritt 3. Backend verified (562 pytest + ASGI endpoint tests, ruff/mypy clean); dashboard typecheck + 41 vitest green. Live research-brief→browser E2E needs the LLM proxy + browser → manual pass pending (→ Plan 008). |
| 007  | `fwbg-agent`-Plugin-Namespace + Suche/Filter — Feature, cross-repo mit fwbg + fwbg-dashboard; fixt tote `POST /api/plugins`-Registrierung | P2 | M | — | DONE (reconciled) — **STOP hit:** `origin/main` shipped Steps 1–2 already (`POST /api/plugins` + user-dir layout) under namespace **`agent-authored`** (client already aligned); local main was 51 commits stale. Per maintainer decision: keep `agent-authored` (not `fwbg-agent`), do gaps only. Done: Step 3 namespace filter in `GET /api/plugins` + Step 4 MCP resolution (fwbg `feat/007-agent-plugin-namespace`, 16 pytest incl. 3 new + live curl E2E green) and Step 6 dashboard source-filter/search/violet badge (fwbg-dashboard `feat/007-agent-plugin-namespace`, typecheck clean). Steps 1–2 upstream, Step 5 client already correct. |
| 009  | Strategie-Loop-Qualität: Trade-Diagnostik, Median-Gate, Phasen-Funnel, Holdout+Kosten-Stress am Promote-Tor, StrategySpec, Event-Study-Vorfilter — cross-repo mit fwbg (WP4, ggf. WP1/WP6) | P1/P2 | L | — | IN PROGRESS — WP1 + WP2 DONE (619 pytest grün). WP1: `orchestrator/trade_diagnostics.py` (Buckets Stunde/Wochentag/Haltedauer/Jahr + Payoff/Streak/Top-5 aus `fold_results.json` `test_trades_detail`, plus fwbgs `trade_analytics` durchgereicht; keine fwbg-Änderung nötig) → Runner-Sidecar `trade_diagnostics.md` → Analyst-Prompt. WP2: Median-Gate; Gate liegt real in `lifecycle._guard_strategy_backtested_to_paper` (Drift ggü. Plan, nicht `validate_and_apply`), Feeder api/runs.py + auto_runner.py + Runner-Payload auf `_median_metrics_across_assets` umgestellt. WP4 (Promote-Tor Holdout + Kosten-Stress) DONE cross-repo: fwbg (Branch feat/009, Commit 461b46c) start_date/end_date/cost_multiplier in StrategyConfig+CLI+API + Daten-Slice + Spread-Skalierung (Spike: kleine Patches, kein STOP; echter Run verifiziert); fwbg-agents holdout_months=24, Runner reserviert Holdout via end_date, neues orchestrator/promote_gate.py (Holdout+Kosten-Stress-Läufe → promote_holdout/promote_cost_stress Criteria → promote_gate_results.json + Events), validate_and_apply(promote) gated, Analyst-Prompt-Slot. WP3 (Phasen-Funnel + Universum-Verengung) DONE: config (reiterate_max_depth→12, universe_narrowing_min_iteration=5, universe_min_size=3), asset_specific-Flag in ResearcherHypothesis + validate_hypothesis (≥3 Assets Erstiteration), Per-Asset-Sharpe-Serie in lineage, deterministische Hard-Rules in recommendations._enforce_universe_rules (Phasengrenze/Floor/Subset). WP5 (StrategySpec + kontrolliertes Vokabular + Lessons-Digest) DONE: speckit/strategy_spec.py (StrategyFamilyLit-Enum + edge_mechanism-Dedup-Anker), ResearcherHypothesis strategy_family→Enum + edge_mechanism + Dims, research_flow schreibt strategy_spec.md, PriorArtMatch.edge_mechanism, orchestrator/lessons.py (data/lessons.md nach Abandon) + Researcher {{ lessons_digest }}. 650 pytest grün. WP6 (Event-Study-Vorfilter, P3) DEFERRED nach Spike: Vorfilter spart nur Analyst-Iterationen (Translator+1 Backtest bleiben), Grenznutzen nach WP1–WP5 klein, lokale Variante birgt Divergenzrisiko → zurückgestellt (falls später: Variante A). WP1–WP5 (P1/P2-Kern) umgesetzt. |
| 010  | Profitabilitäts-Hebel: forexsb→dukascopy-Bugs + Loop-Zuverlässigkeit (WP1), Trial-Zählung/Deflated-Sharpe am Promote-Gate (WP2), Analyst-Tool-Use über per-Trade-Daten (WP4), Researcher-Fan-out+Critic+Diversität (WP3), Regime-Labels+Interventions-Digest (WP5), Crossover+Daten-Scan (WP6) — cross-repo mit fwbg (WP1, ggf. WP5) | P1–P3 | L | 009 | IN PROGRESS — WP1–WP5 DONE (709 pytest fwbg-agents grün, 92+243 pytest fwbg grün; branch `feat/010-profitability-levers` beide Repos, unpushed). WP1: fwbg `_resolve_source()` statt frozen `"forexsb"`-Default in `api/chart.py`/`signal_composer.py` (Commit 8d4bca2); fwbg-agents `translator._normalize_datasource()` (Off-Katalog-Wert bei genau 1 konfigurierter Quelle deterministisch korrigiert statt Lauf zu killen), Researcher `retries={"output":3}`, `llm_max_retries` 1→2 (Commit 6cd19a1). WP2: `orchestrator/trials.py` (Trial-Zählung aus `fwbg_results.json`+Grid-Kombinationen, Deflated-Sharpe-Formel exakt gegen Bailey/López de Prado 2014 Zahlenbeispiel verifiziert ±0.0001) als dritter Promote-Gate-Check neben Holdout/Kosten-Stress (Commits ca1e204/48c9f5d/83031aa); zusätzlich neuer `GET /trials/summary`-Endpoint + fwbg-dashboard zeigt jetzt Deflated Sharpe statt Sharpe im generischen Run-View (Nutzerwunsch, fwbg-dashboard-Repo Commit b36d0fb, TS-Port von trials.py gegen dasselbe Paper-Beispiel verifiziert). WP4: `orchestrator/trade_diagnostics.py` `build_trade_store()`+`query_trades()`/`describe_trades()` — In-Memory-SQLite über alle Trades einer Iteration, Analyst bekommt `query_trades_tool`/`describe_trades_tool` (nur SELECT, 200-Zeilen-Cap via `fetchmany`, `check_same_thread=False` nötig da pydantic-ai Tools im Worker-Thread laufen) (Commit 46094d4). WP3: `researcher_candidates_n` (default 3) sammelt bis zu N valide Hypothesen, neuer `agents/critic.py` (adversarial, `score`+`kill_risks`+`verdict` pro Kandidat, `winner_index` muss auf `pass` zeigen) wählt Gewinner; alle rejected → `ResearcherFanoutExhaustedError` wie bisher; Verlierer-Hypothesen unter `data/agent-runs/<critic_run_id>/losing_hypotheses.json`; neues `orchestrator/exploration_balance.py` (`strategy_family×asset_class×timeframe`-Verteilung) als `{{ exploration_balance }}`-Slot im Researcher-Prompt; `candidates_n=1` bytegleich zum Altverhalten (Commit 4ce33de). WP5: Maintainer-Entscheidung eingeholt (Option A — fwbg berechnet Regime serverseitig); fwbg `attach_regime_labels()` in `nested_cv.evaluate_on_holdout()` (ATR-Tertile `vol_regime`, ADX-Bucket `trend_regime`, geteilt von ML- und Signal-Pfad) (fwbg Commit f736edd); fwbg-agents neue `by_vol_regime`/`by_trend_regime`-Buckets in `trade_diagnostics.md` (erscheinen automatisch auch im WP4-`query_trades`, da `build_trade_store` Spalten dynamisch vereinigt) + neues `orchestrator/interventions.py` (Median-Sharpe-Delta parent→child je `strategy_family×recommendation_kind`, `{{ interventions_digest }}`-Slot im Analyst-Prompt, pro Analyst-Lauf neu berechnet) (Commit 159963a). WP6 (Crossover-Modus + Daten-Scan-Tool, P3/Effort L) bewusst nicht begonnen: Datenvoraussetzung (≥5 Abandons mit Post-Mortem) ist in Produktion längst erfüllt (67 abgeschlossene Linien), aber der Plan verlangt einen eigenen Spike vor Umsetzung (P3, letzte WP in der Reihenfolge) — zurückgestellt für eine dedizierte Folge-Session. |
| 008  | Agent-Run-Timeline — Folgeaufgaben aus 006/007 (`plugin_registered_in_fwbg`- + Szenario-Events, `data/agent-runs/`-Retention, `parent_run_id`-Drill-down, fwbg-Registrierungs-Resync) + ausstehender 006-Manual-E2E | P2/P3 | M | 006, 007 | DONE — Schritte 1–6 DONE (feat/006-agent-run-detail; 576 pytest grün, 1 skipped). Schritt 5 (parent_run_id) via ContextVar-Propagation + Migration 0009 + API-`children` + fwbg-dashboard-Drill-down (autonomer auto_runner-Pfad bewusst ohne parent-Link, siehe Plan-Abschluss). 006-Manual-E2E weiter ausstehend (braucht LLM-Proxy + Browser) |
| 011  | Require FWBG API auth and teach fwbg-agents to send the key | P1 | M | — | DONE (branch `advisor/011-013-hardening` in both repos, unpushed). fwbg-agents: `FwbgClient(api_key=…)` injects `X-API-Key` on the internally-created httpx client (never mutates an injected one); `settings.fwbg_api_key` threaded through all 18 call sites; tests cover header inject/omit + injected-client-untouched. fwbg: `create_app()` now **fails closed** — raises unless `FWBG_API_KEY` is set or `FWBG_ALLOW_UNAUTHENTICATED_API=1` (dev). Middleware already used `hmac.compare_digest` (planned improvement was already present). Tests: fail-closed startup + 401/200 on `/api/plugins`; `conftest.pytest_configure` sets the dev bypass (import-time `app=create_app()` defeats a per-test fixture). docker-compose: key is opt-in via `${FWBG_API_KEY:-}` wired to `api`+`agents`, with `FWBG_ALLOW_UNAUTHENTICATED_API=1` as the safe default on the internal-only `api` service. **Caveat flagged:** the `dashboard` image does not send `X-API-Key`, so setting a key in compose breaks dashboard→api until that (separate repo) lands. 66 fwbg + 17 agents focused tests green; ruff/format/mypy clean. |
| 012  | Validate fwbg strategy filenames before filesystem writes | P1 | S | — | DONE (branch `advisor/011-013-hardening`, unpushed). Single `_strategy_file()` helper in `api/strategies.py` (`validate_id` + `resolve()`/`relative_to()` boundary) now used by get/create/update/commit/history/version/delete. Traversal name in the POST body → 400 with no escaped file; slashed name → 400; invalid `{name}` path segment → 400. **Deviation from plan's literal test:** `../evil` in the URL is normalized by the HTTP client to `/api/evil` (→404, still no traversal) before reaching the handler, so the path-param test uses an invalid single segment instead (documented in the test). 12/12 `TestStrategyEndpoints` green; ruff clean. |
| 013  | Make the fwbg backtest concurrency contract explicit | P2 | S | — | DONE (branch `advisor/011-013-hardening`, unpushed). fwbg default `FWBG_MAX_CONCURRENT_RUNS` 10→1 via testable `_max_concurrent_runs_from_env()` helper + regression test (unset→1, override honored). Documented in fwbg README env table + `.env.example` and fwbg-agents `.env.example`; runner/config docstrings reworded to describe a *configured* single-slot contract, no behavior change. docker-compose already pinned the `api` service to 1. fwbg run-spawn tests + agents runner tests green. |
| 014  | Freeze the holdout per lineage + gate-attempt budget + Analyst sees pass/fail only — cross-repo mit fwbg (halb-offenes Datumsfenster, `cost_multiplier > 0`) | P0 | M | — (vor 015) | IN PROGRESS (executor dispatched 2026-07-16, worktrees `advisor/014-*`) |
| 015  | Durable trial statistics (`trial_stat` table + backfill) + Deflated-Sharpe fail-closed bei NaN | P0/P1 | M | 014 | TODO (wartet auf 014-Review) |
| 016  | Paper-Fidelity: per-Trade-Sharpe + Fill/Spread-Telemetrie & Divergenz-Metriken — cross-repo mit fwbg (`bot.py` telemetry) | P1 | M | — | DONE (reviewed/approved; fwbg-agents `advisor/016-paper-fidelity` Commit `50f4c96`, fwbg `advisor/016-paper-telemetry` Commit `675ffdf` in `/home/haex/Projekte/fwbg-advisor-016`, beide ungemergt). `sharpe_paper_per_trade` (backtest-vergleichbar) + `avg_entry_slippage`/`avg_assumed_half_spread`/`fill_fidelity_ratio`/`fidelity_sample_size` (legacy-safe: None/0); fwbg `_append_trade_entry` bekommt `signal_price` (= letzter gecachter Close zur Entscheidungszeit, wiederverwendet für Slippage-Warnung) + `assumed_spread` (via `get_asset().spread`, Fallback = DEFAULT_ASSETS-Schätzung — genau die Backtest-Annahme). Kriterien-YAMLs nur auskommentierte Beispielregeln (Gate erst nach Kalibrierung). fwbg-Suite in 5 Chunks komplett grün (2678 Items), agents 718 grün + 2 bekannte Worktree-only-Failures. |
| 017  | Design-Spike: Portfolio-/Korrelations-Risk-Layer vor Live (Doku + Feasibility-Script, kein Build) | P2 | M | — | DONE (reviewed/approved; branch `advisor/017-portfolio-spike`, Commit `3e9b20d`, ungemergt im Worktree). Deliverables: `docs/plans/2026-07-portfolio-risk-layer.md` + `scripts/spike_portfolio_correlation.py` (read-only, Mechanik gegen Synthetik-Fixture verifiziert). **Kernbefund:** lokale `data/state.db` hat 0 Strategien mit per-Trade-Daten (3× proposed, keine Transitions) — Gate-Vorschlag „blocked on data density"; 5 offene Fragen mit Empfehlungen im Doc (u. a. warn-and-override statt Hard-Reject, Ceiling 0.6 als Setting, Sizing agents-seitig statt risk_management-Plugin). |
| 018  | LLM-Kosten-Telemetrie: USD-Estimator + `cost_usd`-Backfill + `GET /economics/summary` | P2 | S–M | — | DONE (reviewed/approved; branch `advisor/018-llm-cost-telemetry`, Commit `27054d9`, ungemergt im Worktree). `tools/llm_pricing.py` (Longest-Substring-Match, Opus 4-7/4-8 je $5/$25 per 1M, JSON-Override via `llm_price_table_json`), `cost_usd` an den 5 LLM-Call-Stellen (Tavily-Quota-Pseudo-Call per Review-Entscheid ausgenommen, Kommentar), idempotentes `scripts/backfill_llm_costs.py`, `GET /economics/summary` (by_agent/by_outcome/lineage_top/cost_per_promoted_strategy/`unpriced_calls`). 14 neue Tests, ruff+mypy clean; 2 Worktree-only-Failures (plugin_contract braucht Geschwister-fwbg) per stash als pre-existing bewiesen. Follow-up: staler `LlmCall`-Docstring in `models.py` (out of scope). Merge-Hinweis: `config.py` kollidiert trivial mit 014. |
| 019  | Datenqualitäts-Report beim Dukascopy-Ingest (warn-only `.quality.json`) — nur fwbg | P2 | M | — | DONE (reviewed/approved; fwbg `advisor/019-data-quality` Commit `f852890` in `/home/haex/Projekte/fwbg-advisor-019`, ungemergt). Neues `data/quality.py::assess_bars` (pure; Coverage, Gaps mit Wochenend-Heuristik Fr 20:00→So 23:00 UTC gecappt auf 3 Tage, Monotonie/Duplikate, OHLC-Verletzungen, NaN, zero_volume_ratio) + Verdrahtung in `download()` (`<SYMBOL>_<TF>.quality.json` neben CSV, `quality`-Dict im Result, warn-only — CSV nie blockiert, per Test bewiesen). 9 neue Tests, ruff clean; komplette fwbg-Suite chunked grün bis auf 4 per stash bewiesene pre-existing Order-Pollution-Failures (exit-strategy registry). Hinweis: pandas 3.0 speichert den Index in µs-Auflösung — Delta-Berechnung via `np.timedelta64(1,"s")`, von Tests abgefangen. Follow-ups laut Plan offen: Report beim Backtest konsumieren, Runner/Analyst-Sichtbarkeit, Batch-Revalidierung alter CSVs. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003 depends on 002 only because both edit `pyproject.toml` — land 002
  first to avoid merge conflicts; no functional coupling.
- 005 after 004: both tighten plugin-authoring gates; 004's import allowlist
  changes which fixture plugins exist/pass, so sequencing avoids re-work.
- 001 and 004 are independent and can run in parallel worktrees.
- 011 should land before enabling fail-closed auth in a live deployment: it
  teaches `fwbg-agents` to send `X-API-Key` before `fwbg` requires it.
- 012 is independent and can land before or after 011.
- 013 is independent, but best scheduled after any running experiments finish
  so changing the default backtest slot count does not surprise an active run.
- 015 after 014: both edit `promote_gate.py`, and 015 counts the gate
  attempts that 014 budgets as trials.
- 016 and 019 are independent of each other, but complementary: 019's
  `spread_p90` in the quality report is the backtest-side counterpart of
  016's fill-fidelity ratio.
- 017 (portfolio spike) reads 016's output if available but does not block
  on it.
- Plans 014/016/019 touch fwbg: branch from `origin/main` there — the local
  fwbg checkout sat on a stale feature branch (25 commits behind) at
  planning time.

## Vetted findings NOT planned (candidates for the next round)

- **SEC-03 — No auth on state-changing endpoints** (`main.py:57-73`,
  `api/secrets.py:35`, `api/strategies.py:440`): real, but the right fix
  (API token vs. enforced loopback bind) is a deployment decision the
  maintainer must make first.
- **SEC-04 — Search keys plaintext in `data/secrets.json`**
  (`tools/secrets.py:45-52`): S effort; fold into the auth decision above.
- **COR-05 — Promote-live splits state across two commits**
  (`api/strategies.py:509-527`): real but low-probability; self-documented.
- **PERF-01/02 — Duplicate + serial catalog fetches**
  (`research_flow.py:261` + `translator.py:366`; `live_catalog.py:144-202`):
  verified, S–M effort, good next-round candidate.
- **PERF-03 — Composite index on `agent_run(agent_name, strategy_id, status)`**: verify impact first.
- **DEBT-01/02/03 — AgentRun-envelope + LlmCall duplication; translator god
  module (862 lines)**: do DEBT-01/02 before any translator split.
- **DEBT-04 — Stale `merge_with_db` comment** (`plugin_flow.py:360`): trivial; bundle with any nearby change.
- **TEST-03 — SSE event bus has zero tests** (`events.py`, `api/events.py`): S effort.
- **TEST-02 — `hypothesis` dev dep unused**: decide adopt-or-drop (partially addressed by 002's philosophy; kept for a deliberate decision).
- **DOCS-01/02/03 — README claims "M0 — Skeleton" at v0.16.0; design-doc
  path differs between README and CLAUDE.md; docs/plans/ has no index**: S each.
- **Known open items from the 2026-07-10 code review of `dc84bd6`** (evidence
  in that session's review, all confirmed): (a) evaluator/reiterate failures
  after `plugin_implementer` DONE permanently close the auto add_indicator
  budget with no resume path (`auto_runner.py::_author_and_reiterate` /
  `pick_next_add_indicator_pending`); (b) budget-exhausted BACKTESTED
  strategies get no terminal disposition/post-mortem and are rescanned every
  idle tick; (c) the implementer-DONE gate is not scoped to the current
  sidecar, so a re-emitted `add_indicator_request.json` (manual re-analyze)
  is never auto-drained. All three are policy/design decisions — plan when
  the maintainer picks a direction.

### From the 2026-07-16 re-audit (fwbg-agents `75123b0`, fwbg `origin/main 8ab08f7`)

fwbg:

- **Slug path traversal in `POST /api/plugins`** (`api/plugins.py:48,204`):
  `payload.slug` is unvalidated and built into the write path — arbitrary
  file write. S effort; natural extension of plan 012's `validate_id`
  approach to `plugins.py` (plan 012 covers only `api/strategies.py`).
- **Plugin validation executes submitted code in the API process; empty
  `tests_code` skips the test gate entirely, plugin still registers as
  verified** (`api/plugins.py:68,219-236`): sandboxing is L effort and
  partially by-design (the engine must execute plugins) — needs a maintainer
  decision on isolation level.
- **422 bodies leak full pytest stdout/paths** (`api/plugins.py:234`): S;
  bundle with the auth work (plan 011).
- **IG order confirmation is a single shot** (`adapters/broker/ig/adapter.py:559-606`
  and the near-identical `packages/fwbg-broker-ig/.../adapter.py`): fixed
  0.5 s sleep + one fetch; missing confirmation → `success=True/PENDING`
  (SL attachment unverified); exception after send → `REJECTED` without
  reconciliation (duplicate-position risk on retry, no dealReference
  idempotency); `filled_quantity` = requested, not confirmed size. M effort,
  MED risk, needs demo-account verification. Do after/with the adapter
  consolidation below.
- **Two divergent IG adapters** (765 + 689 lines, both runtime-reachable;
  the packaged one has zero tests; existing tests assert the mocked response
  translation, never the outgoing order payload): consolidate to
  `fwbg-broker-ig` before touching order logic. L effort.
- **`deflated_sharpe_ratio` implemented twice** (agents `trials.py:146`
  stdlib vs fwbg `optimization/overfitting.py:68` scipy, plus a TS port in
  fwbg-dashboard): the number that gates live promotion can drift between
  systems. M — export once via `fwbg-sdk`, cross-check test.
- **fwbg CI runs only ruff-check + pytest** (`deploy.yml:42-45`): mypy is
  configured but never runs; no format check, no coverage. S.
- **Regime labels** (`optimization/nested_cv.py::attach_regime_labels`):
  positional `entry_idx` indexing (misaligns if warmup rows dropped) and
  whole-window quantiles (future-looking) — fine while diagnostic-only;
  document the boundary, switch to timestamp lookup. S.
- **`test_early_termination.py` at repo root** is tracked but outside
  `testpaths` — never collected by CI. S: move or delete.

fwbg-agents:

- **Shared sqlite connection across pydantic-ai worker threads**
  (`trade_diagnostics.py:395-400`, `analyst.py:548`): the "calls are
  sequential" comment is not guaranteed — parallel tool calls in one model
  response share one connection. S: lock or per-call store. Bundle: replace
  the keyword-denylist SQL guard (false positives on string literals,
  `trade_diagnostics.py:416-429`) with `conn.set_authorizer`.
- **`interventions.py:105` `IN()` over unbounded parent-id set**: hits
  SQLite's bound-parameter limit at scale, inside the Analyst path. S: join
  or chunk.
- **`auto_runner.tick()` full-state scans + per-row queries/file-stats every
  tick** (four `pick_next_*` selectors): linear in backlog on a permanent
  loop. M: push predicates into SQL. Related: `auto_runner.py` is now the
  largest module (991 lines) — split selectors/actions when touching it.
- **Second-order prompt injection**: plan 004 framed researcher *tool
  output* as untrusted, but researcher-derived web content flows unframed
  into the Critic prompt (`critic.py:63-71`) and downstream
  Translator/Analyst artifacts. M, defense-in-depth.

## Direction options (maintainer's call — not ranked against bugs)

1. **Live-trading counterpart to the paper flow** — paper has 4 modules
   (`paper_flow.py`, `criteria_paper.py`, `paper_analyst.py`,
   `fwbg_paper_reader.py`); live has only a state + human gate. Once
   LIVE_TRADING, no agent watches the strategy. L effort, HIGH risk,
   needs the ig-bot telemetry contract first (design spike, not a build).
2. **Speckit dedup gate (Phase 2)** — `speckit/` models exist but nothing
   imports them; the source-of-truth doc records the live cost ("Analyst
   requests building duplicates"). M–L effort; each avoided duplicate saves
   a full Planner→Implementer→Evaluator LLM chain.
3. **Close the autonomous loop over paper analysis + calibration** — both
   `paper_analyze` and `calibrate` exist, are tested, and are manual-only;
   `auto_runner.tick` never calls them. M effort; the natural next increment
   for the "autonomous" premise. *(2026-07-16 re-audit: priority has risen —
   with the promote gate and paper machinery in place, the autonomous loop
   still effectively ends at BACKTESTED; also a prerequisite for plans
   016/017 to matter autonomously.)*
4. **Preset crystallization** — fully specified in
   `docs/plans/2026-07-03-preset-crystallization.md`, zero code; the
   catalog + transition plumbing it needs already exists. M effort; two open
   questions in the doc must be answered first (fwbg `_meta` passthrough,
   trigger threshold).

## Findings considered and rejected

- Path traversal via LLM slugs: cleared — strategy slugs are regex-sanitized
  (`hypotheses.py:107-144`), plugin slugs pattern-constrained
  (`plugin_planner.py:69,99`), implementer slug forced equal to plan slug.
- Paper→live human gate missing: cleared — enforced at two layers
  (`api/strategies.py:464-468`, `lifecycle.py:211-218`); residual risk is
  auth (SEC-03), not gate logic.
- CORS wildcard-with-credentials: cleared — origins restricted to localhost.
- SSE bus unboundedness: cleared — bounded `asyncio.Queue(maxsize=200)` per subscriber.
- `scripts/backfill_plugin_specs.py` as dead code: cleared — documented,
  working Phase-1 dev tool.
- Calibrator/universe boundary handling: cleared — safe-float, NaN/inf
  filtering, dedup already present.
