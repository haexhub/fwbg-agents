<!-- spaex-composed:source_hash="9e7b48fcf6e4f38bf7b68abe77960c9f008ba12e8e074b96a44a6c6e713c810a" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- A hand-maintained source file clearly over 500 LoC MUST be split, or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- All changes to an existing Git repository MUST be authored in a dedicated repository-local worktree on a topic branch (a directory registered via `git worktree list`, not a copied directory or unrelated clone), covering source code, tests, documentation, configuration, and generated files that are part of the change. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code MUST be maintained in files separate from production code, in every language, for unit, integration, contract, and end-to-end tests; test bodies and test-only helpers MUST live in dedicated test files, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_

## MUST_NOT

- Do not call blocking I/O or CPU-heavy work directly from an event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not pass a growing collection of positional tuples or unrelated dictionary keys through the application. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not use a bare `except`, catch `Exception` to hide a failure, or return `None` as an undocumented substitute for an error. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Resource cleanup must not depend on reference counting, garbage collection, or a caller remembering a separate close operation. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Do not call `asyncio.run()` from code that may already be running inside an event loop, including in async tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not claim checks passed, or present a green-looking diff as verified, without actually running the project's formatter, linter, type checker, and relevant tests before handoff. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not hide unrelated scenarios inside a large fixture or broad parameter matrix; add a new parametrized case only when it demonstrates a distinct boundary or regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not mock the code under test so heavily that the test only proves its own setup. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not use sleeps to wait for concurrency in async tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Do not author changes in the primary checkout; reserve it for inspection and worktree management. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not enforce a universal maximum line count for functions, files, or pull requests; domain complexity, cohesion, control flow, and the number of reasons to change matter more than a line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not pad files up to 500 lines merely to satisfy the size boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not place inline test modules or test functions in production files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not scatter one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not split a file solely to make it shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- LLM agents MUST NOT leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, and metadata. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Never add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not abstract two things, or introduce a speculative abstraction, merely because they look similar or a change might be reusable; first establish that they share the same behavior, ownership, change reason, and invariants, or that the code evidence supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not add boilerplate nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- Add type hints to public functions, methods, and meaningful data structures; prefer precise types and protocols over `Any`, using `Any` only at a deliberate untyped boundary and narrowing the value before it reaches domain logic; for supported Python versions, prefer modern built-in generics such as `list[str]` and `dict[str, int]`, and use abstract collection types such as `Mapping` for read-only inputs when they describe the contract better. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- At an application's top-level entry point, `asyncio.run()` is appropriate; library code or code already inside an event loop should expose or await a coroutine instead of starting a nested event loop. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- For distributable projects, use `pyproject.toml` for build-system metadata, project metadata, and dependencies, keeping the declared Python version and dependency constraints explicit and using the project's chosen environment or lock workflow consistently. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Model data explicitly, using a small `dataclass`, `Enum`, or dedicated value object when a concept has invariants. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Move blocking I/O or CPU-heavy work off the event loop using an appropriate async API or a worker, keeping async code non-blocking. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Prefer simple, readable control flow; use a normal loop instead of a comprehension when conditions, side effects, or error handling would make the comprehension hard to read. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Raise specific exceptions, catch only exceptions that can be handled at that layer, and preserve the original cause with exception chaining when translating errors. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Use context managers for resources such as files, locks, and transactions. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Validate external input at the boundary and convert it to a domain shape once, so internal functions can rely on their stated types and invariants rather than repeatedly defending against every possible input. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Before handing off a change, run the project's formatter, linter, type checker, and relevant tests (or, absent project-specific commands, a reasonable baseline such as `python -m compileall`, the configured test command, and configured static checks), and report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Follow the repository's established Python tooling and conventions — formatter, linter, type checker, test runner, structure, naming, and size conventions — instead of introducing competing tooling, unless the task explicitly changes that convention. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer the standard library and existing project dependencies before adding a new package; keep any dependency addition justified by a real capability or maintenance benefit, not by a shorter one-off expression, and avoid a new dependency whenever it can be avoided. _[from `com.github.haexmas.atoms.coding-guideline-python/python`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Exercise successful and failure behavior, especially malformed input, missing configuration, expected exceptions, authorization decisions, persistence failures, and resource cleanup, asserting the exception type and meaningful details without over-specifying incidental message formatting. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- For async code, test coroutines with the project's supported async test tooling, and test cancellation, timeout, cleanup, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep each test independent and deterministic, using temporary directories, controlled clocks, seeded randomness, and explicit fakes or mocks at external boundaries. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep mocks narrow and purposeful, mocking an external boundary only when isolation or determinism requires it. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep parametrized cases focused and readable, adding a new case when it demonstrates a distinct boundary or regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Keep tests easy to read, fast enough to run frequently, and honest about what they do not cover, since a passing test is evidence only for the behavior it actually exercises. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Run the project's normal test and static-check commands; at minimum, make sure the test runner discovers the intended files and that a deliberately broken assertion makes the test fail during test development. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Use `unittest` or the project's chosen test framework consistently; fixtures should make setup and cleanup explicit, using context managers and framework cleanup hooks so a failing test cannot leak files, connections, processes, or patches into the next test. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Use focused unit tests for pure logic and isolated components, integration tests for real module boundaries, and end-to-end tests only for workflows that need the complete system, keeping the test level visible in the test location and name according to repository convention. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Add or update tests with behavior changes, testing observable behavior and public contracts rather than private implementation details, so that a refactor preserving the contract does not require rewriting the tests and a test is useful only when it can fail for a real regression. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`, `com.github.haexmas.atoms.general-coding/general-coding`]_
- A file SHOULD have one primary reason to change; split it when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before the first edit, verify the repository root, the active branch, and the registered worktrees, and create a suitable worktree before modifying files if none exists. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments SHOULD explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction, extracting a helper when it gives a meaningful name to a concept, makes a branch independently testable, or removes repeated logic. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable, putting I/O, framework adapters, and process wiring at the edges when the architecture calls for that separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review, bundling production code, relevant tests, documentation, and configuration that describe the same behavior, while splitting independent refactors and unrelated cleanups into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each task isolated in its own worktree and topic branch so that changes can be reviewed, tested, committed, and removed independently. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public module interfaces narrow and intentional, hiding implementation details behind the smallest stable API the callers need. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify, according to the language and repository convention, so a module split does not leave tests, documentation, fixtures, or call sites misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries; pure calculations and decisions should be easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior; a clear name is better than a comment that explains an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Organize code around cohesive domain or feature boundaries where the codebase supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection; when a design is uncertain, make the smallest reversible change that provides evidence about the next decision. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, reducing nesting. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Refactor repeated behavior when the shared rule is real and the abstraction makes both callers clearer, keeping the abstraction close to the domain that owns it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible, since mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a useful review-size warning and a change approaching 1000 lines as a strong prompt to split the work or agree on it in advance, identifying generated code, mechanical renames, and required migrations as legitimate exceptions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use smaller soft thresholds — such as a function beyond roughly 40-60 lines or nesting deeper than roughly three levels — as prompts for review, not automatic failures. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Apply full rigor, without the minimal-code shortcuts otherwise encouraged here, to understanding the problem, input validation at trust boundaries, error handling that prevents data loss, security, accessibility, the calibration real hardware needs, and anything explicitly requested. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Before writing any code, prefer the simplest sufficient solution: check whether it needs to be built at all (YAGNI), then whether it already exists in the codebase, then the standard library, then a native platform feature, then an already-installed dependency, then a one-line expression, before writing new code — but only after reading the task and the code it touches and tracing the real flow end to end. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave one runnable check behind for non-trivial logic — the smallest thing that fails if the logic breaks — written with whatever test tooling the project has already adopted; a project with no such tooling yet gets a framework-free assert-based demo or one small test file instead, and trivial one-liners need no test. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Let the shortest working diff win, but only once you understand the problem — the smallest change in the wrong place is a second bug, not laziness. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark deliberate simplifications that cut a real corner with a known ceiling (a global lock, an O(n²) scan, a naive heuristic) with a `ponytail:` comment naming the ceiling and upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer deletion over addition, boring over clever, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests: confirm whether something simpler already covers the need before building it. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When fixing a bug, fix the shared function at its root cause: grep every caller of the function you touch and fix it once there, rather than patching only the path the report names and leaving a sibling caller still broken. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When two stdlib approaches are the same size, pick the edge-case-correct one; lazy means less code, not the flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Avoid clever metaprogramming when ordinary functions and classes suffice. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Avoid expensive work, network access, and surprising side effects at module import time unless the project explicitly uses that pattern. _[from `com.github.haexmas.atoms.coding-guideline-python/python`]_
- Avoid order dependence, shared mutable module state, real network services, the current time, and arbitrary sleeps in tests. _[from `com.github.haexmas.atoms.coding-guideline-python/python-testing`]_
- Avoid circular imports, hidden global state, and a domain layer that knows about infrastructure details. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid letting a generic utility module become a dumping ground when extracting a shared abstraction. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid re-exporting everything or creating pass-through modules that add no architectural boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_

## MAY

- A language-required test-module declaration or registration MAY remain in the production file, even though test bodies and test-only helpers must otherwise live in dedicated test files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Generated files, vendored code, snapshots, and other files not normally edited by hand MAY exceed the 500 LoC boundary, but should be excluded from size assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
